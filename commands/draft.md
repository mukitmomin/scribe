---
description: Draft a first-person blog post from a learned paper note
argument-hint: "<arxiv-id>"
---

Read `$SCRIBE_HOME/notes/$ARGUMENTS.md` (default `~/.scribe/notes/$ARGUMENTS.md`).
If it does not exist, say so and stop.

Write `$SCRIBE_HOME/drafts/$ARGUMENTS.md`: a 600–900 word first-person blog
draft based on the note. Practitioner voice, concrete claims, no hype words
("game-changing", "revolutionary", etc.). Include code or math only if the
note itself contains it.

Reply with the draft path and a one-line summary of its angle.
