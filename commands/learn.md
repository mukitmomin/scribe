---
description: Learn a paper interactively, section by section
argument-hint: "[arxiv-id]"
---

Use the tutor skill: read `${CLAUDE_PLUGIN_ROOT}/skills/tutor/SKILL.md` and
execute it exactly.

Paper id (optional): $ARGUMENTS
If no id was given, pick the highest-score item in the inbox as the skill
describes.
